import { Button } from 'rsuite';
import { Article, Category } from '@byzantium/graphql-client';

function Home() {
  return (
    <div>
      <h1>Hello, World!</h1>
      <Button>Hello</Button>
    </div>
  );
}

export default Home;
